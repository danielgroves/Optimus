language: ruby
rvm:
  - "2.2.0"
  - "2.2.1"

sudo: true

before_install:
  - sudo apt-get update -qq -y
  - sudo apt-get install -y imagemagick --fix-missing -qq

script:
  - bundle install
  - bundle exec rake test
